"use client";

import Modal from "@/components/Modal";

export default function PokemonPage(props: { params: {id: number} }) {

    return (
        <Modal>
            <h1>Hello WOrld</h1>
        </Modal>
    )
}